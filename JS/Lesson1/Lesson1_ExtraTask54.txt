Создайте переменную именем х. Присвойте переменной х значение 12.  Создайте 
переменную именем у. Присвойте переменной у значение 21.  Напишите 
последовательность команд, в результате которых:  переменные х и у обменяются 
своими значениями. Выведите значения переменных х и у в консоль.

 let x = 12;
 let y = 21;
 x = x + y;
 console.log(x);
 y = x - y;
 console.log(y);
 x = x - y;
 console.log(x);